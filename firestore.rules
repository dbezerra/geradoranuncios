rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regras abertas para usuários autenticados (ajuste depois se quiser)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
